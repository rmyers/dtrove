language: python
python:
  - "2.7"
services:
  - rabbitmq
install:
  # Build/test dependencies
  - pip install -r requirements.txt --use-mirrors
  # Install dtrove
  - pip install -e .
script:
  - fab test:skip_js=True
notifications:
  email:
    - "myer0052@gmail.com"
